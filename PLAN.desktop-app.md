# PLAN.desktop-app.md

This file is intentionally kept only as an archive marker.

The old Windows desktop plan is not the supported direction for the current `ubuntu` branch.

Current branch guidance:

- use `README.md` for end-user Ubuntu setup and usage
- use `AGENTS.md` for agent handoff and implementation guidance
- treat the supported product model as `daemon + CLI + watch + OpenCode sync`

If you are working on this branch, do not use the old desktop plan as the source of truth.
